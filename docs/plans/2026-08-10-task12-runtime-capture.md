# Task 12 — trusted runtime capture coordination

- Status: **REAL WINDOWS PILOT ARCHIVED / LOCAL EVALUATOR GO / FINAL ACCEPTANCE PR AND EXACT MAIN CI PENDING**
- Source baseline: merged main `81f7a956bfe24687671340946c1e2d593277aa6a`
- Product outcome: the real Windows pilot must be assembled from product-observed, plan-bound facts and written through the existing opaque capture/final Archive authority; caller-authored complete Evidence JSON must remain unable to promote itself to a live pilot.

## Deep module and seam

The production seam is one durable pilot capture module. Its eventual external interface starts or resumes one plan-bound capture session, accepts only validated product or explicit operator receipts, reports a path-free next action/status, and finalizes one immutable Archive when the complete frozen plan is satisfied. Sequencing, duplicate detection, cumulative budgets, interruption recovery, projection, privacy checks, and one-shot capture authority remain implementation details behind that interface.

The first prerequisite slice makes Provider accounting trustworthy before the session can consume real task results:

- the qualified Pi JSON adapter counts only assistant `message_end` records from a current live Pi 0.83 stream whose exact terminal tail is `agent_end` then `agent_settled`. A missing, reversed, duplicated, or followed terminal event remains incomplete. Historical receipts that ended at `agent_end` are immutable replay inputs and are never admitted through this live predicate. A controlled interruption instead requires the one-use Core nonce marker plus exact contained-process finality, while the unqualified historical Task 6 process path reports `NOT_PROVEN` because its hidden-retry boundary is not controlled;
- repeated messages inside `agent_end` are not counted again;
- each qualified run uses a one-use Pi configuration snapshot that accepts only exact regular configuration/auth/model files, disables Provider transport retry, Agent auto-retry, and automatic compaction, and removes the snapshot before returning;
- each request must carry a valid Pi usage object whose token and cost totals equal their components;
- request count, total tokens, and Provider-reported cost are retained as provider-neutral `EngineObservation.resourceUsage`;
- cost is conservatively rounded up to integer minor units after aggregation;
- truncated, incomplete, missing, malformed, inconsistent, or overflowing usage is `NOT_PROVEN`, never inferred as zero;
- `hpi-managed-change.v3` requires exact usage for every Agent Attempt, cross-checks its Provider and resource-accounting totals, adds finite per-change token and cost budgets, blocks an unaccounted or unreserved fixback request, and returns `STOP` when usage is missing or over budget. The strict v2 parser remains available for historical replay.
- `hpi-pilot-evidence.v7` separates non-Run Quick receipts from canonical Managed Run Archives and accounts the three required raw-Pi comparator requests, tokens, and Provider-reported cost in the same frozen pilot authorization budget; the earlier v5/v6 hosted receipts remain historical rather than being relabeled.
- Current `hpi-pilot-plan-input.v4` / `hpi-pilot-execution-plan.v4` freeze exactly one deliberate fixback task and exactly one executable acceptance check per task. The fixback task must be a paired Managed task and cannot also be an interruption task. Historical v3 plans retain their explicit plural-check replay schemas.

The second slice connects those facts to a durable production path:

- `FilePilotCaptureCoordinator` opens or resumes one immutable plan-bound session, writes append-only HMAC-linked observations, rejects operation/fact rewrites, persists a signed immutable Quick/raw Provider-operation intent with the complete remaining request/token/cost reservation before each send, and refuses retry or finalization while exact usage is unresolved. It serializes budget-check, Provider execution, and fact publication under one recoverable operation lock, enforces cumulative Provider budgets, reports only path-free status and next actions, and recovers an interrupted final publication without reusing a committed missing Archive;
- `hpi change --run-archive-id <id>` uses the durable Workflow Kernel and Task 9 Archive store, and embeds one strict v4 product task receipt in the canonical Archive instead of relying on terminal output. Pilot execution additionally requires `--pilot-plan <file> --pilot-task-id <id> --pilot-session-id <id> --pilot-operation-id <id>`. The capture session must already be open, Provider-backed execution fails before reservation unless the actual host is Windows x64, and the confirmation states the full frozen Provider request/token/cost allowance. A module-private runtime bound to the exact bundled Engine Host writes one immutable full-remaining-budget intent at the Engine external-operation boundary, after repository/target/Writer-Lease/Engine/process-lease/launch-plan/private-runtime-snapshot preflight and immediately before the qualified Pi process can start. The default process runner cannot bypass that callback. The Host registers a deferred pending identity before adapter or callback code, so concurrent and callback-reentrant exact sends coalesce around one authorization and execution. Any exception after authorization but before a final receipt stores an immutable `UNKNOWN` tombstone; replay returns it and reconciliation remains mandatory. Each qualified process-runner instance binds lease, session, and operation IDs to its opaque owner identity. A clean pre-boundary rejection releases the workspace lease, so a later CLI invocation uses fresh IDs and reaches authorization again; an unreleased lease still blocks on the shared resource. The same capture intent covers every bounded Attempt and prevents a second send when the frozen cumulative request budget is exhausted. If authorization fails before sending after the durable pilot Run and first Attempt have been written, an exact observation-free rerun resumes those facts only after a read-only capture check proves that no Provider intent exists; portable Plan Evidence hashes the retained canonical projection rather than a newly timestamped reconstruction. Later, mismatched, non-pilot, or unknown post-boundary durable state fails closed. A planned forced interruption is claimed before the adapter call but consumed only after authorization succeeds, so a pre-send rejection cannot silently turn its retry into a normal request. Deterministic Change/Plan/Run identities include the frozen pilot plan, task, exact path-free `targetId`, check, session, and operation binding so repeated request definitions remain distinct. The target ID is propagated through the internal execution binding, v4 receipt, resolved Oracle, capture validation, and v7 Evidence alongside the other target fingerprints. The receipt binds the exact canonical plan check ID, plan/task/target, capture identity, portable artifact, product source, Engine release, Provider endpoint/model, credential scope, workflow checklist, and planned interruption;
- `hpi change --pilot-interruption <kind>` is available only with that durable Archive path. The frozen kinds are `FORCED_PROCESS_KILL`, `TERMINAL_CLOSE_SIMULATION`, and `POWER_LOSS_SIMULATION`; they map to distinct process-policy, user-request, and timeout cancellation receipts after the trusted Agent-end marker. The two simulation labels intentionally do not claim a physical terminal or whole-machine outage. All three preserve one Checkpoint and recover through a new Attempt in the same Run;
- `hpi pilot capture managed-task` reads that exact canonical package, replays its kernel history, requires the same session/operation Provider intent, binds repository/source/reference/canonical-check-ID/mode/outcome/Provider-use and installed-runtime facts to the frozen task oracle, and derives its Run chain, Archive fingerprint, named workflow-fact coverage, and automatic-intervention count. For the predeclared fixback task it requires two linked Attempts with immutable `FAIL` then `PASS` Verification receipts and preserved failure history. The live v2 path accepts no caller-authored metrics;
- Managed review now fingerprints Git-visible changed content and existing ignored content, freezes the exact interrupted/fixback workspace across recovery, compares the workspace before and after every independent Verification command, and rebinds the final branch plus target-reference fingerprint even when another branch points to the same commit. An ignored-file mutation, verification side effect, recovery-window drift, or final target-reference drift blocks readiness;
- each full working-tree snapshot uses one cumulative content-inspection budget: at most 8 GiB of newly hashed regular-file or symbolic-link-target bytes and 120 seconds elapsed, with product callers permitted only to tighten those ceilings. Exceeding either budget fails closed with `WORKING_TREE_INSPECTION_BUDGET_EXCEEDED`; no ignored content is silently omitted, and unchanged content is reused only through the exact stat-safe digest cache;
- the exported generic record schema and capture CLI reject caller-authored `TASK_CHAIN` and `RAW_PI_COMPARATOR` JSON. Product observations require a module-private runtime capability, while Managed tasks additionally require canonical Archive replay. A complete Evidence object, a path to an Archive, or an untrusted task/comparator summary cannot promote itself to live Evidence;
- finalization still requires the complete frozen observation set and runs only on Windows through the existing private runtime capability and append-only pilot Archive store.

## Remaining capture work

1. Bind installation, performance, Plugin, update, privacy/storage, review, and exact hosted CI receipts to their real product/operator sources. The packaged runtime now exposes a path-free memory sample, and Quick/Managed overhead excludes Provider runtime.
2. Finalize through `PilotEvidenceCaptureFinalizer` and `FilePilotArchiveStore`, then evaluate the exact trusted Archive.
3. Run the frozen ten-task Windows pilot in disposable worktrees for two explicitly selected repositories under one finite Provider request/token/cost scope, including the three product-derived raw comparators and distinct interruption paths.

## Verification policy

- RED → GREEN tests cover exact accounting, inconsistent totals, duplicate avoidance, propagation through the Engine Host, missing-accounting STOP, finite-budget STOP, and no unaccounted retry.
- Public contract changes update the Task 12 plan and user workflow documentation in the same commit.
- Focused suites, lint, typecheck, formatting, full local tests, package/clean-install smokes, compiled Pi probe, PR CI, and exact merged-head main CI must pass before this slice is treated as complete.
- No test or synthetic receipt changes the daily-use disposition. Only a complete real Windows Archive can produce Task 12 `GO`.

The first full local verification of the durable-capture slice passed 64 test files / 589 tests, strict compilation, build, and formatting, then retained a package-smoke `E404`: the hand-maintained tarball list omitted the newly required `@hunter-pi/managed-change` workspace and npm correctly attempted a public lookup. Package and clean-install smokes now discover the complete workspace set automatically. The current product-path verification first retained one post-review formatting-only failure after 66 files / 639 tests and build had passed. After formatting that exact Archive change, a fresh complete `npm run verify` passed 66 test files / 639 tests, lint, typecheck, strict compilation, build, formatting, all 13 internal package tarballs, the single CLI artifact, every `apps/*` and `packages/*` clean-install manifest, and the compiled provider-independent Pi probe. PR run `31343355260` then retained a deterministic Windows/Ubuntu lint failure: a new cross-workspace import omitted its TypeScript project reference, while stale local declaration output had hidden the omission. The repaired gate starts by cleaning all build output and has a policy test requiring every internal workspace dependency to have a direct project reference; the complete repaired tree passes 66 test files / 640 tests and the same strict build, package, clean-install, and Pi-probe gates. All earlier failures remain part of the record; a replacement hosted PR run and exact merged-head CI are still required for this slice.

The runtime-binding/fixback hardening continuation passes one fresh clean-first `npm run verify`: 67 test files / 682 tests, strict compiler smoke, clean build, formatting, all 13 external package smokes, the single bundled CLI smoke, clean npm installation, and the Provider-independent Pi public-interface probe. Its expanded 189-test focused gate additionally proves that caller-authored pilot runtimes are rejected before Engine work, current plans reject an unexecutable multi-check task while historical v3 plural-check plans remain replayable, path-free target IDs cannot be aliased at the CLI, internal Managed, Quick, or raw-comparator physical-repository boundaries and survive canonical receipt replay, resumed Plan Evidence matches the retained projection, the canonical plan check ID survives Archive replay, a one-request pilot budget prevents a second Managed send, Provider-backed execution is Windows-x64-only, the confirmation exposes the full frozen Provider allowance, durable intent is published at every adapter's final external-operation boundary, local Quick/raw and runtime-snapshot failures leave no unresolved intent, exact replay and concurrent or callback-reentrant identical sends do not reauthorize, failed Fake authorization leaves no recorded operation, post-authorization exceptions preserve an immutable `UNKNOWN` tombstone, a fresh qualified runner can retry after a clean pre-boundary failure without lease-operation replay conflict, an exact durable pilot pre-send Attempt remains recoverable only after a no-intent capture proof, equivalent non-pilot and unknown post-boundary state fail closed before a second send, authorization rejection does not consume a planned interruption, repeated task definitions receive distinct deterministic Run identities, and canonical `NOT_PROVEN` Verification history remains serializable. The added Archive recovery coverage expands the current Task 9 platform contract from its historical 90-test baseline to an exact 97-test matrix across the same eight files; the platform Evidence definition and its contract test bind that new count. This local proof does not replace replacement PR CI, exact merged-head main CI, or the ten-task real Windows Archive.

The first authorized real Provider operation on merged source `81f7a956bfe24687671340946c1e2d593277aa6a` crossed the external-operation boundary and produced the expected allowed disposable-repository file, but the live adapter rejected Pi 0.83's trailing `agent_settled` event as `OBSERVATION_INVALID`. No task fact or exact usage receipt was published after the durable intent, so request, token, and cost usage remain unknown and that original capture session is permanently blocked from retry or finalization. The failure and its immutable identifiers are retained in [the dated attempt record](../validation/2026-08-10-task12-real-provider-attempt-01.md); a later successful pilot must use repaired source, a new exact artifact, plan, session, and operation identities and must not overwrite this history.

The test-first Pi 0.83 terminal repair first reproduced five broad-tail false positives, then retained a second review finding for repeated complete terminal pairs. The final shared live predicate requires exactly one `agent_settled`, as the last record, immediately preceded by `agent_end`; Quick, raw comparator, and Provider accounting consume that same predicate. Missing, reversed, adjacent or separated duplicate, and followed settlement streams all fail closed. The final independent review found no P0/P1/P2 issue, the 2-file focused gate passes 38 tests, and one fresh clean-first `npm run verify` passes 67 test files / 693 tests plus strict compilation, build, formatting, all 13 external package smokes, the single CLI artifact smoke, clean npm installation, and the Provider-independent Pi public-interface probe. Hosted PR CI and exact merged-head main CI remain required before a repaired artifact may enter the replacement pilot.

The exact merged source `60ce642bfc6dd9b7db3f5fccd64dddc7841c32c4` passed all six jobs in main CI run `31418170014`, and its local portable bundle matched the stamped artifact fingerprint. A pre-Provider relocation smoke then found a separate daily-use blocker: the generated launcher forced the packaged CLI working directory to the active release directory. The artifact assembled beneath the source repository could therefore appear usable by discovering that parent Git repository, while an exact complete copy in a standalone installation directory failed repository inspection before Pi launch. No replacement capture session was opened and no Provider request was sent. The disposable failure record remains preserved; the test-first repair requires the launcher to preserve the operator working directory and must pass a standalone copied-installation TUI smoke before another pilot can begin.
