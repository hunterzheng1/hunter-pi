# Initial qualified-release rollback repair

## Real Pilot finding

The exact merged candidate `eeef8e7a45f64af034be7220913c37bcc970b2ed` passed its local portable checks and merged-head Windows/Ubuntu CI, then entered disposable Task 12 capture session `pilot-session-20260811-eeef8e7a`. The first update to qualified candidate `release_hunter-pi-0.1.0-dev.0-16bd5f4b1295` applied successfully and passed a real-terminal Safe Mode smoke without a Provider request.

The immediate rollback to the installed baseline was safely blocked by operation `op_update-rollback-a3d80943-3e14-4e4d-8fb5-5f67d3851212` with `rollback target is not a known applied qualified candidate`. The active update remained usable and no Provider budget was consumed. This is a product defect, not acceptable Task 12 Evidence: a portable release is activated before a new user's append-only update journal exists, so the manager could not discover the otherwise installed baseline as a rollback candidate.

The failed capture remains incomplete and superseded. It is not retried, finalized, or counted as acceptance.

## Repair contract

- The Windows portable adapter can return an installed candidate only after re-verifying its exact artifact bytes and extracted file tree.
- Rollback re-reads the installed candidate and retained Evidence for both journal-history and fallback targets. The fallback is eligible only when no previously applied journal candidate exists, an append-only `APPLY` receipt proves the target was previously active, and the latest earlier `QUALIFY/APPLIED` entry binds the exact candidate identity. A same-release-ID replacement or merely staged candidate is ineligible.
- The normal channel, qualification status, verifier fingerprint, unsigned-preview, Pi self-update, and release identity gates still apply.
- CI output remains `NOT_PROVEN` until a separate metadata-only `QUALIFY` operation obtains the exact hosted Evidence itself. The official command accepts an installation root and main CI run ID, not a caller-authored `PASS` candidate. It performs one run query and one named artifact download, then requires the exact repository/main push/workflow/source, all six required jobs, hosted/local bundle bytes, and hosted/local non-qualification candidate identity.
- `QUALIFY` binds an operation ID derived from the complete canonical request, a path-free target, deadline, and fail-closed elapsed timeout shared by both fixed `gh` calls. It writes an intent before metadata mutation, syncs and atomically publishes strict Evidence without replacement, retains the intent through Receipt acknowledgement, reconciles interruption under the original operation identity, and serves exact replay or an already-qualified same-source invocation without another GitHub query.
- Qualification changes only `portable-release-candidate.json` and the matching version directory's `.hpi-candidate.json`; the update bundle and executable payload retain their original fingerprint.
- Missing, unqualified, identity-drifted, or damaged initial releases remain ineligible for rollback. Verification failure preserves the currently active release and appends a failure receipt.

## Local evidence

The first test was written RED against the real portable adapter shape: a qualified initial version was staged and activated before manager journal creation, a second version was applied, and rollback was blocked. A second RED proved that a qualified release which was only staged could otherwise be selected as the fallback. The first repair head `a5e566152582981e0ab482932f8987ba44c8a964` and PR #75 run `31451189405` passed all six hosted jobs, but review found that its standalone mutator trusted arbitrary `PASS` JSON, had no update operation/journal/reconciliation contract, could expose raw paths on failure, and used a hand-maintained immutable-field projection. That head was not merged.

The first review-hardened replacement, commit `bb5431f`, removes the public direct-write API. Its focused matrix passed 5 test files / 72 updater/portable/CLI tests, including nine exact authority/operator tests for hosted run and artifact binding, non-main or incomplete-run rejection, source and byte-drift rejection, immutable hosted-candidate rejection, local replay with no second GitHub access, durable Evidence, both direct-restart and manager-caught interruption recovery, first update/rollback success, staged-only rollback rejection, tamper rejection, and path-free script failure. Lint and type checking passed. That commit was not pushed while independent review was pending.

The complete local verification passes 70 test files / 705 tests, lint, type checking, strict compiler smoke, production build, formatting, all 13 external package smokes, single-artifact CLI smoke, clean npm installation, and the provider-independent Pi public-interface probe. The first sandboxed invocation retained one environment-only failure because sandbox policy denied Git access to the user-level ignore file and emitted stderr inside a temporary-repository cleanliness test; the uninterrupted rerun under normal machine permissions passed every gate.

Independent standards review of `bb5431f` found that first-rollback lookup did not reload retained Evidence, the adapter removed its intent before the manager Receipt, the operator reused a date-only operation identity with a changed deadline, Evidence publication could leave a partial final file, and the two `gh` calls did not share one elapsed timeout. Its separate Spec-axis attempt was interrupted before reading the diff and is explicitly not counted as a clean review. The follow-up repair proves Evidence-deletion rejection, pre-Receipt and post-Receipt interruption recovery, non-replacing atomic Evidence publication, request-bound operation identities, local same-source no-op, and a decreasing shared timeout. Its focused matrix passes 5 files / 74 tests, including 11 authority/operator tests; lint, type checking, and formatting pass. A fresh complete local verification and replacement dual-axis review are `PENDING` at this point in the record.

The complete local verification of follow-up commit `71736c4` passed 70 files / 707 tests and every remaining local gate. A subsequent valid bounded Standards/Spec rereview inspected every changed path and found that APPLY-history rollback still bypassed retained Evidence; the Standards axis additionally found that raw run text with leading zeroes produced a non-canonical operation ID. RED tests reproduced both. The next revision revalidates installed Evidence and exact journal identity for history candidates and derives the operation ID from the numeric run ID. Its focused 5-file / 74-test gate, lint, type checking, formatting, and diff check pass. Final complete verification and delta rereview are `PENDING` at this point in the record.

Delta review of `210d5b7` confirmed the history-Evidence and numeric-run fixes, then found that fallback still bound only the previous release ID. A RED fixture replaced the installed candidate's qualification with a different valid hosted Evidence identity under the same release ID and reproduced the unsafe rollback. The repair now requires the latest exact `QUALIFY/APPLIED` candidate entry before the prior-active APPLY proof and compares the installed candidate to it completely. A fallback without that identity proof is `BLOCKED`; identity or Evidence drift is `FAILED`, and the current release remains active. Final focused, complete, and delta-review gates are `PENDING` at this point in the record.

Delta Spec review of `ce66749` passed, while Standards review found that the operator stored `QUALIFY` under the portable installation and the production CLI stored APPLY/rollback under the user profile, so the required proofs could never coexist outside the unit fixture. A production-factory RED test reproduced the split: CLI replay could not find the promotion Receipt. The repair introduces one shared path resolver and uses `.hpi-update/manager` from both entry points; the production CLI factory now replays the exact promotion operation locally. Final focused, complete, and delta-review gates are `PENDING` at this point in the record.

Final source commit `9585c11` passed both bounded Standards and Spec delta reviews with no P0–P3 findings. The focused updater/portable/production-CLI matrix passes 5 files / 74 tests. The exact clean local `npm run verify` passes 70 files / 707 tests, lint, type checking, strict compiler smoke, production build, formatting, all 13 external package smokes, single-artifact CLI smoke, clean npm installation, and the provider-independent Pi public-interface probe. Replacement PR CI, exact merged-head CI, live artifact promotion, and the complete replacement Pilot remain `PENDING`.

The superseded first repair tool also processed the full 64 MB `eeef8e7a` bundle and allowed the disposable fixture to return to `eeef8e7a`; that historical recovery remains useful for preserving the failed Pilot fixture but is not accepted as final qualification Evidence. The final candidate must be rebuilt and qualified through the replacement live-run path.

Remote PR CI, exact merged-head CI, a newly promoted artifact, and a complete replacement Task 12 Pilot are `PENDING`. Daily-use acceptance remains `NOT_PROVEN` until those gates actually pass.

## Exact hosted closure and live timeout finding

PR #75 head `7ed539b4d17b8a2d9d67864b487542644d335c3f` passed all six required jobs in run `31464679828` without a rerun. It merged as `c38f6cca014934146d526654aaa56e4ff8b1c217`, whose exact push/main run `31466777673` independently passed Windows, Ubuntu, Pi + Task 9 + Task 10 Evidence, both Task 7 containment jobs, and Task 7 Evidence. The exact main job generated and uploaded the named Windows portable artifact.

A clean local package of that merged source produced release `release_hunter-pi-0.1.0-dev.0-c38f6cca0149`, artifact `sha256:c98eeaebd9d9f845cf63908d1be11d81a986f98606c56a1e0ab8754869e38934`, and 64,350,863 bundle bytes. Its packaged qualification correctly remained `NOT_PROVEN` before the live operator ran. The first operator invocation returned the fixed `BLOCKED` shell response before a qualification journal entry existed; a later no-network argv probe proved that npm had retained the absolute path as one argument, so no path-splitting explanation is claimed.

A controlled direct invocation then reached the real GitHub observer but remained running beyond the shared 120-second budget. Read-only process inspection found the operation's `gh` process still alive; the operator terminated only that invocation. No candidate metadata was promoted, no qualification Evidence or terminal journal Receipt was written, and no Provider request was made. The pre-retry quota snapshot was healthy at REST core 4,921/5,000 and GraphQL 4,953/5,000, so this is a Windows subprocess-finality defect under unstable GitHub transport, not evidence of an active API rate limit.

The root implementation used graceful `child.kill()` at timeout and resolved only on a later process close. The first RED started a real CLI child plus descendant and required bounded return, empty fail-closed output, and both processes gone. An interim repair used a separate POSIX process group or the system-owned Windows `taskkill.exe /T /F`, with `SIGKILL` fallback, for timeout and output overflow. Its focused updater/qualification/portable/CLI matrix passed 5 files / 75 tests. One sandboxed complete verification retained a Task 7 fixture failure because sandbox policy denied Git access to the user-level ignore file; the exact 11-test file then passed under normal machine permissions without a code change. A fresh complete normal-permission `npm run verify` passed 70 files / 708 tests, lint, type checking, strict compilation, production build, formatting, all 13 external package smokes, the single CLI artifact smoke, clean npm installation, and the provider-independent Pi public-interface probe.

Two independent bounded reviews rejected that interim process repair as final. Killing by a captured parent PID after launch could miss an inherited-pipe descendant after the parent had exited, and PID reuse could target an unrelated process. The test kept the parent alive and therefore did not reproduce that case. The nominal 1 MiB limit was also applied separately to stdout and stderr, and exporting the helper from the qualification module exposed an unintended arbitrary-process API through the updater barrel.

Replacement REDs made a parent-plus-descendant tree retain both inherited output handles, exceeded one shared limit with 700,000 bytes on each stream, and asserted that the updater package exports neither process runner name. The first platform-driver repair composed the existing execution platform driver: Windows owns the target tree in a kill-on-close Job Object from launch, while Linux uses the existing subreaper process-tree helper. It accepted output only after exact identity, terminal phase, empty tree, closed streams, no termination cause, and a real exit code; every other observation returned null/empty. Its qualification file passed 12 tests, its seven-file process/update matrix passed 98 tests, and a fresh complete normal-permission `npm run verify` passed 70 files / 710 tests plus every remaining gate.

The next bounded Standards and Spec reviews independently found that platform-helper setup still preceded the runner's target-only timeout, so a later successful second `gh` result could cross the shared deadline. They also found that the test's referenced child kept its parent alive and then used unsafe bare-PID cleanup, and that the hand-edited lockfile put the new dependency on the execution workspace itself rather than updater. New REDs inject delayed platform setup and require an exact `TIMEOUT` cancellation, return a nominally successful artifact download after the shared elapsed deadline, and explicitly exit the parent after unreferring a detached inherited-pipe descendant with its own finite self-exit insurance. The test no longer sends any signal by recorded PID. The runner now starts a monotonic deadline before driver creation, actively cancels when it expires, and rejects a settlement observed at or after that deadline; the observer rechecks its shared operation deadline after each fixed `gh` call. `npm install --package-lock-only` confirms the corrected updater-to-execution dependency edge without an execution self-cycle. The qualification file passes 14 tests and the seven-file matrix passes 100 tests. A fresh complete normal-permission `npm run verify` passes 70 files / 712 tests, lint, type checking, strict compilation, production build, formatting, all 13 external package smokes, the single CLI artifact smoke, clean npm installation, and the provider-independent Pi public-interface probe. Narrow Standards and Spec rereviews both report clean with no remaining P0–P3 finding.

Before replacement remote gates, one read-only quota check reported REST core 5,000/5,000 and GraphQL 4,913/5,000. Qualification remains fixed at one run view plus one named artifact download with no polling or silent retry, and CI observation uses one watch connection rather than concurrent status loops. Replacement PR/main CI, a newly qualified exact-main artifact, and the full Pilot remain `PENDING`; `c38f6cca` is not accepted as a qualified daily-use candidate.

PR #76 first run `31479365512` retained a Windows unit-test failure while Ubuntu passed every quality gate. The new detached-descendant test gave the complete qualification runner a three-second absolute deadline; under the full hosted Windows test load, the platform helper correctly exhausted that deadline before starting the target, so the test-only PID handoff file never existed. This is not evidence of a leaked target tree, and the run is not rerun unchanged. The replacement test leaves the separately injected setup-deadline proof intact, gives the real platform driver its production-bounded setup allowance, proves the parent has exited while the detached inherited-pipe descendant is alive, and only then writes a test-owned trigger that makes the descendant exceed the shared output ceiling. That policy cancellation must empty the Job Object/subreaper tree before the runner returns. The qualification file passes 14 tests, the seven-file matrix passes 100 tests, and the exact hosted `test:ci` selection passes 58 files / 588 tests locally with lint, type checking, formatting, and diff checks. Replacement hosted gates are `PENDING` at this point in the append-only record.

## Exact-main qualification attempt and reproducibility finding

Replacement PR #76 head `0bb6c546f4fa33f612862d14dc0b57fbab5c8d6c` passed all six required jobs in run `31480802866`. It merged as `2383a48a2317a98710c3d69994477a73897b73ff`; exact push/main run `31482983415` passed the same six jobs without a rerun. The pre-qualification quota snapshot was healthy at REST core `4937/5000` and GraphQL `5000/5000`.

The first exact-main local package remained `NOT_PROVEN`, with artifact `sha256:f4d1cce3592aa4b4c98d2acac45da239452d3aca831908153e875ea41add8031` and 64,350,866 bytes. Its sole qualification operation `op_update-qualify-31482983415-b888d4b1ee10c48d` returned an append-only `FAILED` Receipt without changing candidate metadata. A bounded read-only diagnosis proved that the run metadata was exact and all six jobs passed. The named hosted artifact download completed only after the old 120-second shared allowance, and its bundle was `sha256:b4579f3abecaf6bbc382aa8e17d9706d6d0290c73cd933d45571c398480b9aef` with 64,788,665 bytes. Decoding both bundles found the same 18,757 paths and exactly one changed file: local `node.exe` was Node 24.14.0 (`sha256:63c259c8…`, 91,380,224 bytes), while floating hosted `node-version: 24` selected another patch binary (`sha256:ac51903c…`, 92,540,232 bytes). This is a release reproducibility defect plus an undersized transfer budget, not API rate limiting.

RED tests now require one exact patch version in `.node-version`, require all four hosted setup steps to consume that file, require the packer to reject any other runtime, and require one artifact-size-derived shared qualification budget. The first repair passed focused tests and a complete local verification of 70 files / 714 tests plus every remaining gate. Independent review then found that Actions' default hidden-file exclusion omitted `.hpi-update/active.json`, the installed `.hpi-candidate.json`, and `.hpi-artifact`; the timeout used the 65 MB inner bundle rather than the observed 377,795,154-byte visible upload tree; `.nvmrc` still floated at Node 24; and the canonical status header was stale. Those findings are retained rather than treating the first repair as final.

Replacement REDs require `include-hidden-files: true`, exact agreement between `.nvmrc` and `.node-version`, and a timeout derived from the complete physical local upload tree. The policy remains finite and fail closed: three-minute minimum, 60-second setup allowance plus transfer allowance at 1 MiB/s, eight-minute maximum, the existing ten-minute operation deadline, and pre-network rejection of links, non-files, more than 100,000 entries, or more than 2 GiB. Focused GREEN, fresh complete verification, replacement review, PR/main CI, exact local/hosted byte equality, live qualification, and the complete Pilot remain `PENDING` at this point in the append-only record.

The next Standards rereview required direct tests for every new upload-tree safety boundary. RED coverage now proves empty-tree, redirected-root, nested junction/symlink, non-file classification, entry-count, byte-count, and successful complete-tree sizing behavior. That rereview then found that strict canonical-path string equality rejected a valid Windows path with case-only variation. A Windows RED reproduced the false block; the repair compares resolved roots with platform path semantics, retaining parent-junction divergence detection while accepting case-only aliases. The final focused qualification/package matrix passes 3 files / 21 tests. A fresh clean-first `npm run verify` passes 70 files / 716 tests, lint, type checking, strict compilation, production build, formatting, all 13 external package smokes, the single CLI artifact smoke, clean npm installation, and the Provider-independent Pi public-interface probe. Final bounded Standards and Spec reviews are clean with no remaining P0–P3 finding. Replacement PR/main CI, exact local/hosted byte equality, live qualification, and the complete Pilot remain `PENDING`.

## Reproducible exact-main attempt and CLI resolution finding

PR #77 head `b2e7c4522d270d99781d93fccf82508042af4f38` passed all six required jobs once in run `31492283609`. It merged as `3fdee9510fbdd0b9eb5ceea6e386690e5520ca80`; exact push/main run `31495005154` passed the same six jobs once. Two independent local packages from that exact main commit produced the same 64,350,858-byte bundle at `sha256:6f645474c4ea76aded6580cd6156b31255fe69812793b320b746d63acdb0b066` and the same non-qualification candidate identity. The pre-operation quota snapshot was healthy at REST core `4999/5000` and GraphQL `4889/5000`.

The sole live qualification operation `op_update-qualify-31495005154-dc25d72f09c6b81e` retained an append-only `FAILED` Receipt and left both candidate copies at `NOT_PROVEN`; it published no qualification Evidence and consumed no Provider budget. The complete 439,541,428-byte local upload tree selected the finite 480,000-millisecond process budget, but the operation failed before that timeout. One read-only named-artifact diagnosis then proved that hidden manager state was present and that the hosted bundle's bytes, fingerprint, and non-qualification candidate identity exactly matched local. Exact run metadata likewise bound push/main, source `3fdee951…`, workflow `CI`, and all six successful jobs.

The remaining failure was local and deterministic: the Windows Job Object helper calls `CreateProcessW` without a shell and therefore does not resolve the bare executable name `gh`; its exact platform error was `CREATE_PROCESS_2`. Direct shell invocations had hidden this mismatch. RED tests reproduced the failed short-lived unqualified command. An initial GREEN that probed PATH with Agent-process filesystem calls was superseded after independent review found that a disconnected path entry could outlive the hard deadline and that PATHEXT fallback was ambiguous. The replacement repair performs one exact `$PATH:gh.exe` lookup through system `where.exe` inside the existing Job Object, passes it the same remaining timeout, accepts only an absolute matching native filename, and reuses that result for both GitHub calls. No shell, script extension, or unbounded Agent-process filesystem lookup is involved. A second Standards review required negative resolver coverage and a composition-level proof. The resulting matrix rejects nonzero lookup exit, stderr, relative output, and mismatched basename; the Observer test proves one lookup, exact executable reuse, and decreasing shared timeouts. Focused qualification GREEN passes 1 file / 22 tests; the complete platform file passes 9/9, with lint and type checking also green. Full verification, replacement review, PR/main CI, a newly built candidate, live qualification, and the complete Pilot remain `PENDING`.

Three complete replacement verification attempts passed clean, lint, and type checking, then each reached 69/70 files and 723/724 tests before one unrelated Task 7 fixture rejected its initial clean temporary Git repository with `source identity command did not complete cleanly`. This happened before the test created the unrelated file it is meant to reject. The exact Task 7 platform file passed 11/11 in isolation, while two direct full Vitest runs passed 70/70 and 724/724 without a source change. The distinction was ambient Git state: `git status` inherited the default user `core.excludesFile`, whose inaccessible path could produce stderr, and a configured exclude could also hide the very unrelated file the gate is meant to catch. A new RED proved that false clean deterministically. The repair invokes status with command-scoped `core.excludesFile=/dev/null`; native Git for Windows accepts that path, Ubuntu uses it directly, repository ignore semantics remain intact, and the focused Task 7 file returns to 11/11. All three failed aggregate attempts remain superseded, not converted into passes; one fresh aggregate replacement remains required.

That fresh clean-first replacement then passed all 70 files / 724 tests and every remaining local gate. The required post-build no-network executable probe nevertheless failed before target launch. A bounded manual helper diagnosis retained no private path or credential content and identified the exact cause: this host supplies equal-valued `PATH` and `Path` keys, which PowerShell 7 refuses to deserialize as one object. Existing platform fixtures passed because they intentionally supplied a minimal environment. A new Windows RED reproduced the duplicate-case failure; the generic Job Object driver now canonicalizes schema-valid ASCII environment names case-insensitively with deterministic lexicographic-first selection before serializing the helper request. Because product source changed again, the preceding aggregate PASS was superseded.

The rebuilt compiled probe then resolved the exact absolute `gh.exe`; its first target launch returned exit 1 only because the restricted diagnostic sandbox could not read the existing GitHub CLI configuration outside the workspace. The same compiled code was rerun with that local read boundary available and no network operation: `gh --version` returned exit 0, empty stderr, and `gh version 2.92.0 (2026-04-28)`. This proves local executable resolution and contained launch, not GitHub authentication, API availability, artifact qualification, or Pilot readiness.

The next clean-first verification passed 70 files / 725 tests and every remaining local gate, and the exact post-build compiled probe passed again. Both final reviewers nevertheless found the same P1 before submission: the new tenth platform test made the formal Task 7 parser's frozen nine-check report shape invalid, so hosted containment Evidence would deterministically fail without a retry. That local PASS is therefore superseded. The duplicate-environment RED now runs as an independent Windows quality test; the formal Task 7 file remains nine tests, and the combined four-file focused matrix passes 43/43. Fresh aggregate verification, final rereview, hosted CI, qualification, and Pilot remain required.

The replacement final state passes a fresh clean-first `npm run verify`: 71 files / 725 tests, strict compilation, production build, formatting, all 13 external package smokes, the single-artifact CLI smoke, clean npm installation, and the Provider-independent Pi public-interface probe. The hosted-style quality selection passes 59 files / 601 tests, and the post-build compiled no-network probe again resolves absolute `gh.exe`, returns `gh version 2.92.0 (2026-04-28)`, exit 0, and empty stderr. Final bounded Standards and Spec rereviews report no remaining P0–P3 finding. Replacement PR/main CI, a newly built exact-main candidate, live qualification, and the complete Pilot remain `PENDING`.

## First current-verifier qualified release

PR #78 head `8b81803303ada2946d4d7eedde696cdf8ddadd21` passed all six required jobs once in run `31513591159`. It merged as `ad4c91605718930a72b454992cb9bc42aa3b00f5`; exact push/main run `31516463825` passed Windows and Ubuntu quality, Pi + Task 9 + Task 10 identity Evidence, both Task 7 containment jobs, and Task 7 identity Evidence. The Windows Task 7 job retained one policy-eligible first-attempt failure and its single bounded retry passed; the first receipt was not relabeled or discarded.

Two independent local packages from that exact clean main source produced the same 64,350,978-byte bundle at `sha256:4230ef1d6c6c3ab7386bb8ae3295513eae8b69131b3a170611f859c0ad806ee4`, byte-identical portable Manifests, and the same candidate identity after excluding the expected pre-qualification observation time. The pre-operation quota snapshot showed that the earlier rate limit had reset: REST core was `4997/5000` and GraphQL was `4987/5000`.

The only live qualification operation for candidate A, `op_update-qualify-31516463825-800c49f6897ae38c`, returned `APPLIED`. It published strict Evidence `evidence_main-ci-31516463825-portable`, bound source `ad4c91605718930a72b454992cb9bc42aa3b00f5`, all six exact successful jobs, and candidate identity `sha256:f430eba8d7f14e1a46690efa04a770c2de8dddfd32022d6047bd304041c81744`. Candidate A now has `qualification=PASS`; its append-only manager journal and physical qualification Evidence agree with the candidate. Candidate B remains the independently reproduced `NOT_PROVEN` copy and was not silently promoted.

An exact standalone copy retained the bundle fingerprint and returned update `READY` under a new isolated profile. Running that copy against unrelated pre-existing user state instead returned the generic fail-closed update block; the copied installation files were byte-identical, and the new profile removed the state-identity conflict. Fresh setup then detected the configured Provider authentication metadata without reading or logging credential material. No Pilot capture session has been opened and no Provider request has been sent from this release.

Task 12 still requires two distinct update candidates with real `APPLIED` update and rollback observations. Older candidates bind the superseded verifier and include the historical rollback defect, so they cannot be used to manufacture those observations. Two documentation-only main release points will therefore be qualified with the current verifier: candidate A and the next release will be the two update candidates, and the following release will be the fresh Pilot baseline. Daily-use acceptance remains `NOT_PROVEN` until that final baseline completes the full real Archive and evaluates to `GO`.
