# Pi 0.84.1 与 Windows Release 验证

日期：2026-08-13

## 当前结论

状态：`LOCAL_FULL_GATE_PASS / REMOTE_CI_PENDING / RELEASE_PENDING / NEW_DAILY_USE_NOT_PROVEN`

Hunter Pi `0.1.0-dev.1` 固定 `@earendil-works/pi-coding-agent@0.84.1`。本轮只验证 Provider-neutral 公开接口、打包、安装、更新与回滚合同；不执行真实 Provider 请求，不操作真实用户仓库或现有用户状态。0.83 的历史 Task 12 `GO` 不自动迁移为 0.84.1 的日常使用结论。

## 本地结果

- 升级前基线：71 个测试文件、728 个测试通过。
- Pi RED：精确候选仍为 0.83.0，未满足 0.84.1 身份和 delta-only 合同，按预期失败。
- Pi GREEN：Windows 临时 Git fixture 中 Extension、JSON、RPC、SDK 为 `SUPPORTED`；JSON/RPC `message_update` 仅保留 `assistantMessageEvent`，没有累计 `message` 和 `partial`，最终 assistant `message_end` 存在；真实 Provider 为 `NOT_PROVEN`。
- npm production audit：0 个已知漏洞。
- 安装 RED：`install.ps1`、ZIP/校验资产和 CI 安装闭环缺失，4 项按预期失败。
- 安装 GREEN：唯一来源脚本、本地目录安装、本地 ZIP 安装、内嵌/独立脚本字节一致、SHA-256、payload fingerprint、篡改拒绝、稳定 `bin\hpi.cmd`、进程 PATH 幂等、旧 `hpi` 冲突保留、不同活动 release 覆盖拒绝和既有 Hunter Pi 状态隔离通过。
- CLI 单制品 smoke：Hunter Pi `0.1.0-dev.1`、Pi `0.84.1`，raw Pi 未改变。
- portable adapter：版本目录激活、资格检查、更新、回滚和已知 release 篡改拒绝回归通过。
- 完整本地门禁：一次 fresh clean-first `npm run verify` 通过 73 个测试文件、733 个测试，以及 lint、类型检查、严格编译、正式构建、格式检查、13 个外部包 smoke、单制品 CLI smoke、干净 npm 安装和 Pi 公共接口探针。

精确 main CI、GitHub Release 和发布后真实 URL 安装仍为 `PENDING`，将在完成后追加精确提交、run、tag、asset SHA-256 和隔离安装 Receipt。

## 发布边界

发布形态仍是未签名 `developer-preview`。通过本轮门禁最多证明：固定 Windows x64 制品可下载、校验、安装、启动，并保留 Provider-neutral 更新/回滚合同。它不证明代码签名、Stable、广泛插件兼容、真实 Provider 可靠性、任意真实仓库安全、生产就绪或新的日常使用 `GO`。
