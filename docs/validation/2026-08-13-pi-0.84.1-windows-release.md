# Pi 0.84.1 与 Windows Release 验证

日期：2026-08-13

## 当前结论

状态：`PI_UPGRADE_VERIFIED / WINDOWS_UNSIGNED_PREVIEW_RELEASED / PROVIDER_NEUTRAL_REAL_USE_PASS / NEW_DAILY_USE_NOT_PROVEN`

Hunter Pi `0.1.0-dev.1` 已固定 `@earendil-works/pi-coding-agent@0.84.1`。精确源码的 Windows/Ubuntu CI、Windows x64 portable 资格、GitHub prerelease、发布后下载、隔离安装、稳定命令、版本、更新状态、一次实际更新与回滚、disposable Git fixture 检查，以及离线 TUI 启动均已通过。

2026-08-13 收尾复核时，Pi 官方 GitHub latest Release 和 npm `latest` 仍均为 `0.84.1`；仓库 lockfile 的精确版本和 npm integrity 与升级评估一致。

因此，这个版本可以在 Windows x64 上作为 unsigned `developer-preview` 下载、安装并进行 Provider-neutral 试用。它尚未使用 Pi 0.84.1 发起真实 Provider 请求；Provider 认证在隔离状态中按预期为 `BLOCKED`。本记录不声明 Stable、生产就绪、任意真实仓库安全或新的日常使用 `GO`。Pi 0.83 的历史 Task 12 `GO` 不迁移到本版本。

## 精确身份

| 项目 | 值 |
| --- | --- |
| 产品 | Hunter Pi `0.1.0-dev.1` |
| Pi Engine | `@earendil-works/pi-coding-agent@0.84.1` |
| Node runtime | `24.14.0` |
| 源码提交 | `d9f2d931b9fc42d23ceae60fada2aee811caf2ec`，`CLEAN` |
| 标签 | `v0.1.0-dev.1`，解析到上述源码提交 |
| Release | [Hunter Pi 0.1.0-dev.1](https://github.com/hunterzheng1/hunter-pi/releases/tag/v0.1.0-dev.1) |
| 发布类型 | GitHub prerelease，非 draft，未签名 |
| portable release ID | `release_hunter-pi-0.1.0-dev.1-d9f2d931b9fc` |
| update bundle | 64,936,745 字节；`sha256:5091110764aa5e7499f4b00e9e9b800cab6d739a17303d8b547dd8827186b983` |

## 本地实现与门禁

- 升级前基线为 71 个测试文件、728 个测试通过。
- Pi RED 先证明旧候选不满足 0.84.1 身份和 delta-only 合同；安装 RED 先证明 `install.ps1`、ZIP/校验资产和 CI 安装流程缺失。
- Pi GREEN 在临时 Git fixture 中验证 Extension、JSON、RPC 和 SDK 为 `SUPPORTED`。JSON/RPC `message_update` 只包含有类型的 `assistantMessageEvent` delta，不再依赖累计 `message` 或 `partial`；最终 `message_end.message` 仍是完成判断和 Provider 用量的权威消息。
- 当前探针写入严格 v2 收据。Pi 0.83 的历史 Evidence 只通过单独的严格 v1 reader 读取，不作为 0.84.1 验收。
- Windows 安装器验证唯一来源脚本、本地目录、ZIP 和远程安装、外层与逐文件 SHA-256、payload fingerprint、严格 candidate/active/portable 身份、复制后复验、篡改拒绝、重解析点和 8.3 路径别名拒绝、8,300 文件上限、用户 PATH 幂等与失败回滚、稳定 `bin\hpi.cmd`、旧命令冲突保留和既有状态隔离。
- 活动运行时会复验完整版本目录。Pilot/Managed Change 从活动指针、活动 candidate、原始 artifact 和已解压文件读取当前身份；根目录保留的 bootstrap manifest 不冒充当前版本。
- portable adapter 的资格、激活、更新、回滚、失败恢复、历史 Evidence 重读和已知 release 篡改拒绝通过。
- `npm audit --omit=dev --audit-level=low` 返回 0 个已知漏洞。
- fresh clean-first `npm run verify` 通过：73 个测试文件、748 个测试、lint、类型检查、严格编译、生产构建、格式检查、13 个外部包 smoke、单制品 CLI smoke、干净 npm 安装和 Provider-independent Pi 0.84.1 公共接口探针。
- 最终独立代码复核未发现剩余 P0–P3 问题。

## Hosted CI 与资格 Evidence

失败历史保持追加，不改写为通过：

| Run | 精确源码 | 结果 | 处置 |
| --- | --- | --- | --- |
| `31636499639` | `e2c7812` | Windows portable 在旧的 15 分钟 ZIP 预算处取消 | 保留；为该作业设置有界的实际预算 |
| `31637984422` | `5feb825` | portable 已生成；Windows PowerShell 7 自动转换 ISO 时间戳，安装验证失败 | 保留；按字符串读取严格 JSON 日期 |
| `31640187879` | `11ccb05` | 精确 CI 全部通过；旧六作业资格合同拒绝新的 12 作业集合 | 原提升目录保留 `FAILED` journal；修复后用副本回归并得到 v2 `APPLIED`，但不发布该制品 |
| [`31643808274`](https://github.com/hunterzheng1/hunter-pi/actions/runs/31643808274) | `d9f2d931b9fc42d23ceae60fada2aee811caf2ec` | `success` | 最终发布依据 |

最终 run 的 12 个预声明资格作业全部成功：Windows/Ubuntu tests、Windows/Ubuntu quality、Windows/Ubuntu Task 7 containment、Windows portable、Windows external package、Windows clean locked install、Task 7 Evidence、Pi + Task 9 + Task 10 Evidence 和 `CI gate`。`Change scope` 和跳过的文档作业不属于资格集合。

提升操作写入已提交的 [`hpi-windows-portable-qualification-evidence.v2`](evidence/pi/windows-portable-qualification-31643808274.json)：

- Evidence ID：`evidence_main-ci-31643808274-portable`；
- 安装内原始 Evidence SHA-256：`648be8ee159fa7f7c48ac13c0cb6fa12be494ba4b0f47d1f0a962cf8cb16fdaa`；仓库格式化副本 SHA-256：`72e8ea7a715f6000bc67ddc57eda88cd58c8e72c1d97ac273c5e9277cc98ea77`，严格解析后的 JSON 值相同；
- run attempt：`1`；run conclusion：`success`；
- observed/qualified time：`2026-08-12T21:52:32Z`；
- candidate qualification：`PASS`。

## Release 资产

Release 于 `2026-08-12T22:19:53Z` 发布。GitHub 返回的资产 digest 与本地发布目录完全一致：

| 资产 | 字节数 | SHA-256 |
| --- | ---: | --- |
| [`hpi-windows-x64.zip`](https://github.com/hunterzheng1/hunter-pi/releases/download/v0.1.0-dev.1/hpi-windows-x64.zip) | 241,421,081 | `70e1902c487d8cccc9a2f45a7a953601feb736a0350f9c749708b487396a858c` |
| [`hpi-windows-x64.zip.sha256`](https://github.com/hunterzheng1/hunter-pi/releases/download/v0.1.0-dev.1/hpi-windows-x64.zip.sha256) | 86 | `5f0c02e7367cb8223b8dc379768e105cca7e36f7a0218f79758daf4706b62ac3` |
| [`install.ps1`](https://github.com/hunterzheng1/hunter-pi/releases/download/v0.1.0-dev.1/install.ps1) | 48,691 | `73e930469b110e2d1ead735afae8f59f688dadab0397547faf8b16dc81d05181` |

Release 的 `install.ps1` 与检出源码字节相同。GitHub raw tag 会按 LF 返回文本，而 Windows 检出和 Release 资产为 CRLF；规范化换行后的三者内容相同。第一次发布后验证在执行安装前拒绝这个原始字节差异，未放宽制品校验。第二次验证明确分离文本来源一致性和 Release 资产原始字节一致性后继续执行。

## 发布后真实 URL 安装

验证从 GitHub raw tag 和 GitHub Release URL 重新下载安装器，并由安装器从精确 Release URL 下载 ZIP 与 `.sha256`。环境使用包含空格和中文的临时路径、独立 `HUNTER_PI_HOME`、无远端 disposable Git fixture，以及移除 ambient Node.js、npm、Pi 和 `hpi` 的 PATH。

结果：

- 安装返回 `INSTALLED / Source=REMOTE / Checksum=VERIFIED`；
- `stableCommandReady=true`，稳定 `hpi.cmd` 在干净 PATH 中解析；
- `hpi version --json` 返回产品 `0.1.0-dev.1`、Pi `0.84.1`、精确源码提交和 `CLEAN`；
- `hpi update status --json` 返回 `READY` 和精确 portable release ID；
- disposable Git fixture 的 `hpi pilot target` 返回 `READY`；
- 安装和 fixture 检查均未执行 Provider 请求，也未触碰现有 Hunter Pi 用户状态。

### 更新与回滚

另一个隔离 portable 根从已资格化的 `0.1.0-dev.0 / Pi 0.83.0` 基线开始：

1. `update check` 对最终 candidate 返回 `AVAILABLE`。
2. `update apply` 返回 `APPLIED`；活动身份变为 `0.1.0-dev.1 / Pi 0.84.1 / d9f2d931... / CLEAN`，状态为 `READY`。
3. `update rollback` 返回 `APPLIED`；活动身份恢复为 `0.1.0-dev.0 / Pi 0.83.0 / b174ac7...`，状态仍为 `READY`。

资格、APPLY 和 ROLLBACK 分别保留为 journal sequence 1–3。整个过程不使用 Provider。

### 实际 TUI

在发布后安装和 disposable Git fixture 中运行 `hpi setup`，只确认数据披露；未执行 `hpi login`。随后在真实 TTY 中运行 `hpi smoke tui`：

- Pi 显示 `v0.84.1` 和 Hunter header；
- Safe Mode 为 `ON`，`PromptInput=BLOCKED`；
- `/hunter-status` 返回 `HunterStatus=DETECTED Command=/hunter-status`；
- 使用 Ctrl+D 干净退出并写入人工 smoke acknowledgement；
- 最终 `interactive_tui=DETECTED`，`ProviderRequests=NOT_PROVEN`。

Pi 在离线启动时还输出 `fd not found. Offline mode enabled, skipping download.`。该告警没有阻止本次 Core 加载、命令显示或退出；其他依赖 `fd` 的交互功能未在本次 smoke 中声明为已验证。

在仅保留稳定 `hpi`、Git 和 Windows 系统目录的 PATH 中，ambient `node`、`npm` 和 `pi` 均不可见。最终 Doctor 的 Node runtime、临时 Git fixture、Engine、配置、披露、Core 和 TUI 检查均为 `DETECTED`；`overallStatus=BLOCKED` 的唯一原因是隔离状态没有 Provider 认证。这是本轮禁止凭证和真实 Provider 请求的预期结果。

## 可用性边界

当前可以真实执行的范围：

- Windows x64 下载、校验、安装、重复安装和稳定命令启动；
- Provider-neutral 的版本、Doctor、fixture preflight、TUI smoke、资格更新和回滚；
- 固定 Pi 0.84.1 的 Extension、JSON、RPC 和 SDK 公共接口；
- Quick Session 和 Managed Change 的实现及自动化合同测试。

当前仍未证明：

- Pi 0.84.1 的真实 Provider 登录、请求、费用、可靠性和恢复；
- 本版本在真实用户仓库中的完整日常工作，以及新的 Task 12 `GO`；
- 代码签名、Stable 渠道、生产就绪、广泛第三方插件兼容或 OS 级隔离；
- 非 Windows 平台的同等级安装与日用验收。

因此，当前建议仅在 disposable 或可恢复的测试仓库中评估。只有 `hpi doctor --json` 无阻断、重要仓库已有远端备份、工作树干净且开发者准备人工审查变更时，才进入后续单独授权的真实使用验收。
