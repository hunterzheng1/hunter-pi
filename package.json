{
  "name": "hunter-pi",
  "version": "0.0.0",
  "private": true,
  "description": "Hunter Pi engineering workspace",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25",
    "npm": ">=11.0.0 <12"
  },
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json --clean && tsc -b tsconfig.build.json --pretty false && node scripts/bundle-cli.mjs",
    "clean-install-smoke": "node scripts/clean-install-smoke.mjs",
    "compare:pi-evidence": "npm run build && node dist/tools/compare-pi-probe-evidence.js",
    "compare:task7-evidence:compiled": "node dist/tools/compare-task7-platform-evidence.js",
    "compare:task7-evidence": "npm run build && node dist/tools/compare-task7-platform-evidence.js",
    "doctor": "node tools/doctor.ts --json",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings=0",
    "package-smoke": "npm run build && node scripts/package-smoke.mjs",
    "package-smoke:compiled": "node scripts/package-smoke.mjs",
    "pack:preview": "npm run build && node scripts/pack-developer-preview.mjs",
    "pack:preview:compiled": "node scripts/pack-developer-preview.mjs",
    "pack:windows-portable": "npm run build && node scripts/pack-windows-portable.mjs",
    "probe:pi": "npm run build && node dist/tools/pi-public-interface-probe.js",
    "probe:pi:compiled": "node dist/tools/pi-public-interface-probe.js",
    "probe:task7": "npm run build && node dist/tools/task7-platform-probe.js",
    "probe:task7:compiled": "node dist/tools/task7-platform-probe.js",
    "strict:check": "node scripts/strict-compiler-smoke.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "verify": "npm run lint && npm run typecheck && npm test && npm run strict:check && npm run build && npm run format:check && npm run package-smoke && npm run clean-install-smoke && npm run probe:pi"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.3",
    "esbuild": "0.28.1",
    "eslint": "10.8.0",
    "globals": "17.9.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vite": "8.2.0",
    "vitest": "4.1.10"
  }
}
